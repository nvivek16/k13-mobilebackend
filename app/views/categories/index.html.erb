<h1> Listing categories</h1>
<table>
	<tr>
		<th>Name</th>
	</tr>
	<% @categories.each do |category| %>
	<tr>
		<td><%= link_to category.name,category_events_path(category)%></td>
	</tr>
	<%end%>
</table>
<br/>
<%=link_to "create a new category",new_category_path %>
